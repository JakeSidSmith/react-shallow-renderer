export const elementSymbol = Symbol.for('react.element');
export const memoSymbol = Symbol.for('react.memo');
export const fragmentSymbol = Symbol.for('react.fragment');
export const portalSymbol = Symbol.for('react.portal');
export const providerSymbol = Symbol.for('react.provider');
export const contextSymbol = Symbol.for('react.context');

export const MATCHES_CLASS = /^class\s/;
export const INVALID_ELEMENT_ERROR_MESSAGE =
  'Invalid or unsupported React element / child';
